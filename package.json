{
    "name": "cloud-compute-website-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "cd Frontend-Astro && npm run build && rm -rf ../.vercel && cp -r .vercel ../",
        "install": "cd Frontend-Astro && npm install",
        "start": "cd Frontend-Astro && npm run dev"
    }
}